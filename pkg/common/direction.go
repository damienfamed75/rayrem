package common

// Direction is used to tell which way an entity is facing.
type Direction int

// In world coordinates, -1 is typically going to represent left and 1 right.
const (
	Left  = -1
	Right = 1
)
